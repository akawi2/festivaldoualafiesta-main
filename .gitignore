# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env files with real deployment secrets — .env.example stays committed
.env
.env.production

# Local-only production data snapshot (candidate photos, ID cards, parental
# authorizations...) used to seed the local self-hosted Supabase for testing.
# Never commit real personal documents — see supabase-selfhost/README.md for
# the automated data-seed/storage-seed alternative used on actual deploys.
storage-export/
dbdoualafiesta.zip

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
